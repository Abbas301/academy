import { CasestudiesfilterPipe } from './casestudiesfilter.pipe';

describe('CasestudiesfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CasestudiesfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
