import { BatchfilterPipe } from './batchfilter.pipe';

describe('BatchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BatchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
